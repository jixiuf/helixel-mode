## Developing Helixel Mode

Run the linter and fix any byte compilation errors or documentation
inconsistencies (don't forget to clean up the output afterwards: `make
clean`):

```
make lint
```

Run the test suite:

```
make test
```
